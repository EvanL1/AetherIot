---
title: "典型用户旅程"
description: "从对话操作物理空间、评估边缘运行时、生成应用或连接云端，选择与当前任务匹配的最短路径。"
updated: 2026-07-16
---

# 典型用户旅程

请选择与当前角色和任务匹配的最短路径。所有写操作始终默认拒绝，并且必须经过权限、确认、校验和审计。

## 通过智能体描述物理空间目标

完整的最终用户对话闭环仍是产品方向。当前测试版支持范围更窄、边界明确的工作方式：

1. 把智能体连接到在线文档和默认只读的 MCP 接口。
2. 要求智能体先检查运行时、当前 Pack、公开能力和实时状态，再提出任何变更。
3. 查看需求中的哪些部分可以映射到已实现查询或受治理命令，哪些能力尚未提供。
4. 只为具体任务临时启用写会话，保留确认与审计证据，任务结束后恢复只读。
5. 通过智能体解释结果或撤销带版本的变更，不直接修改 SHM、SQLite 或内部服务。

未来版本将补充类型化意图与方案契约、历史回放仿真、带有效期的临时行为和持续效果评估。完整目标流程见 [AI 原生平台](/overview/ai-native-platform/)。

## 评估本地边缘运行时

1. 打开 [AetherEdge 产品概览](/aetheredge/)。
2. 启动不包含设备和外部服务的安全空组合。
3. 检查运行时健康状态和机器可读清单。
4. 只有应用确实需要时，才添加协议适配器和领域 Pack。

## 构建由智能体生成的边缘应用

1. 从正在运行的 AetherEdge OpenAPI 契约生成客户端。
2. 从只读功能开始，并保留数据质量、新鲜度、拓扑代次和配置版本字段。
3. 只使用经过认证的应用边界，绝不直接写入 SHM 或 SQLite。
4. 只有明确声明权限、确认、幂等和审计行为时，才能添加受治理命令。

## 把边缘节点连接到云端

1. 在[兼容性矩阵](/compatibility/version-matrix/)中选择经过验证的产品版本组合。
2. 分别验证 Edge 和 Cloud 中通过摘要锁定的 AetherContracts 消费者锁。
3. 按照[边缘、契约与云端联动教程](/tutorials/edge-contracts-cloud/)完成开发验证。
4. 在所有公开发行门槛通过前，CloudLink 继续保持实验性，并保留旧版兼容路径。

## 实现独立客户端或运行时

1. 阅读 [AetherContracts 产品概览](/aethercontracts/)。
2. 按照规范语义和封闭 Schema 实现协议。
3. 执行公开测试夹具和黑盒 TCK。
4. 只报告已经取得的互操作证据，不能把它扩大解释为生产部署或生产认证。

## 使用 AetherEMS

当目标是能源管理解决方案，而不是通用边缘平台时，使用 AetherEMS。AetherEMS 提供能源领域语义、规则和工作流，三个平台核心产品继续保持行业中立边界。
